/* exported getValue */
/*
create a function with loop inside to check all values of argument
give a condition to loop and return the argument's property
*/
function getValue(object, key){
    return object[key];
  }
