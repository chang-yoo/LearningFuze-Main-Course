/* exported setValue */

/* create a function with the object, key, value inside the parameter
since object[key] = value return that
*/
function setValue(object, key, value){
  return object[key] = value;
}

console.log(setValue(object, key, value))
